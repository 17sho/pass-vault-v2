import test from'node:test';import assert from'node:assert/strict';globalThis.btoa=s=>Buffer.from(s,'binary').toString('base64');globalThis.atob=s=>Buffer.from(s,'base64').toString('binary');const c=await import('../public/crypto.mjs');test('条目及 vault key 可往返且改密只需重包',async()=>{const salt=c.randomSalt(),old=await c.kek('correct horse battery staple',salt),vault=await c.newVaultKey(),item={title:'秘密',body:'正文',tags:[]},encrypted=await c.encryptItem(vault,item),wrapped=await c.wrapVault(vault,old),unwrapped=await c.unwrapVault(wrapped,old);assert.deepEqual(await c.decryptItem(unwrapped,encrypted),item);const newer=await c.kek('a different strong password',c.randomSalt()),rewrapped=await c.wrapVault(unwrapped,newer),v2=await c.unwrapVault(rewrapped,newer);assert.deepEqual(await c.decryptItem(v2,encrypted),item)})