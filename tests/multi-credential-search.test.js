import test from 'node:test';import assert from 'node:assert/strict';import{rankSearchResults,searchableFields}from'../public/fuzzy-search.mjs';
test('account search matches every credential username but never passwords',()=>{const items=[{id:'multi',type:'account',platform:'Mail',loginUrl:'',credentials:[{username:'first-user',password:'never-find-this'},{username:'second-user',password:'also-secret'}],notes:'',tags:[]}];assert.deepEqual(rankSearchResults(items,'second-user',searchableFields.account).map(x=>x.id),['multi']);assert.deepEqual(rankSearchResults(items,'never-find-this',searchableFields.account),[])});
