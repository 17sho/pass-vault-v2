# v1.1.9 多账号 TDD 证据

- RED：`node --test tests/multi-credential-contract.test.js` — 按预期失败：`normalizePlain` 尚未导出。
- GREEN：同一命令 — 4/4 通过；旧结构规范化、新结构边界、未知敏感字段、website 隔离。
- GREEN：`node --test tests/multi-credential-contract.test.js tests/multi-credential-search.test.js` — 5/5 通过；跨行账号搜索且密码不可搜索。
- 回归（聚焦）：`node --test tests/fuzzy-search.test.js tests/contract.test.js tests/multi-credential-contract.test.js tests/multi-credential-search.test.js` — 14/14 通过。
- 质量门：`npm run lint`、`npm run typecheck`、`npm run build` 通过。
- 全套首次运行发现旧 UI 测试仍按精确标签“账号/密码”定位；已为首行保留兼容可访问名称。该长套件在修复后尚未完整复跑，因此不得发布。
